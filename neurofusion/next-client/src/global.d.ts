interface Window {
  CustomSubstackWidget: {
    substackUrl: string;
    theme: string;
    placeholder?: string;
    buttonText?: string;
    colors?: {
      primary: string;
      input: string;
      email: string;
      text: string;
    };
  };
}

// import { relayInit } from "nostr-tools";

// export const relay = relayInit("ws://127.0.0.1:6969");

// relay.on("connect", () => {
//   console.log(`connected to ${relay.url}`);
// });
// relay.on("error", () => {
//   console.log(`failed to connect to ${relay.url}`);
// });
